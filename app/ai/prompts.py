import json

MUSCLE_GROUPS_COMBINED = [
    "грудь и руки",
    "спина и плечи",
    "пресс и корпус",
    "ноги и икры",
]

MUSCLE_GROUPS_SINGLE = [
    "грудь",
    "спина",
    "ноги",
    "плечи",
    "руки",
    "пресс",
    "икры",
    "предплечья",
]

PRO_MUSCLE_GROUPS_MEN = [
    "Плечи",
    "Спина",
    "Широчайшие",
    "Трапеции",
    "Грудные — верх",
    "Грудные — середина",
    "Грудные — низ",
    "Пресс",
    "Руки — бицепс",
    "Руки — трицепс",
    "Предплечья",
    "Квадрицепсы",
    "Бицепс бедра",
    "Икры",
    "Ягодицы и поясница",
    "Кардио",
    "Общий комплекс",
]

PRO_MUSCLE_GROUPS_WOMEN = [
    "Ягодицы",
    "Бёдра",
    "Внутренняя поверхность бедра",
    "Икры",
    "Пресс и косые мышцы живота",
    "Плечи",
    "Спина (верхняя часть)",
    "Руки — бицепс",
    "Руки — трицепс",
    "Кардио",
    "Общий комплекс",
]

PRO_MUSCLE_SETS_MEN = [
    ["Грудные — верх", "Руки — бицепс"],
    ["Спина", "Широчайшие"],
    ["Грудные — низ", "Руки — трицепс"],
    ["Квадрицепсы", "Бицепс бедра"],
    ["Плечи", "Трапеции"],
    ["Пресс", "Кардио"],
    ["Икры", "Предплечья"],
    ["Ягодицы и поясница", "Икры"],
    ["Общий комплекс"],
]

PRO_MUSCLE_SETS_WOMEN = [
    ["Ягодицы", "Бёдра"],
    ["Внутренняя поверхность бедра", "Икры"],
    ["Пресс и косые мышцы живота", "Кардио"],
    ["Плечи", "Руки — трицепс"],
    ["Спина (верхняя часть)", "Руки — бицепс"],
    ["Общий комплекс"],
]

WORKOUT_SPLITS = {
    1: ["общий комплекс"],
    2: ["верх", "низ"],
    3: ["грудь", "спина", "ноги"],
    4: ["грудь", "спина", "ноги", "плечи"],
    5: ["грудь", "спина", "ноги", "плечи", "руки"]
}

SPLIT_DESCRIPTIONS = {
    1: "Общий комплекс (1 тренировка в неделю)",
    2: "Верх/Низ (2 тренировки в неделю)",
    3: "Грудь/Спина/Ноги (3 тренировки в неделю)",
    4: "Грудь/Спина/Ноги/Плечи (4 тренировки в неделю)",
    5: "Грудь/Спина/Ноги/Плечи/Руки (5+ тренировок в неделю)"
}

TIP_TYPES = [
    "фитнес и технике выполнения упражнений",
    "правильном питании и гидратации",
    "восстановлении и сне",
    "мотивации и достижении целей",
    "профилактике травм и разминке"
]

def build_wellbeing_workout_structured_prompt(
    user: dict,
    wellbeing_reason: str,
    avoid_exercise_names: list[str] | None,
    split_info: str,
    superset_info: str,
    workout_history_info: str,
    exercises_info: str,
) -> str:
    # ВАЖНО: в wellbeing-режиме AI сам выбирает группы мышц, исходя из состояния.
    avoid_block = ""
    if avoid_exercise_names:
        safe_list = [x.strip() for x in avoid_exercise_names if isinstance(x, str) and x.strip()]
        if safe_list:
            avoid_block = (
                "\n\nИЗБЕГАЙ ПОВТОРОВ (это было в предыдущем варианте этой тренировки):\n"
                f"{', '.join(safe_list[:18])}\n"
                "Сгенерируй новый вариант тренировки, который заметно отличается упражнениями и/или их порядком.\n"
            )
    return f"""Ты профессиональный фитнес-тренер. Составь тренировку СТРОГО в формате JSON (без Markdown, без пояснений).

КОНТЕКСТ САМОЧУВСТВИЯ:
Пользователь не может выполнить полноценную тренировку по причине: {wellbeing_reason}.
Составь альтернативную тренировку с учётом этого ограничения (послабления, безопасные варианты упражнений, сниженная интенсивность).
{avoid_block}

ДАННЫЕ ПОЛЬЗОВАТЕЛЯ:
Цель: {user.get('goal', 'не указано')}
Уровень: {user.get('level', 'не указано')}
Ограничения (анкета): {user.get('health_issues', 'нет')}
Место занятий: {user.get('location', 'не указано')}
Частота тренировок (из анкеты): {user.get('workouts_per_week', 'не указано')} раз/нед
Время на тренировку: {user.get('workout_duration', 'не указано')}
Оборудование: {user.get('equipment', 'не указано')}
Форматы: {user.get('workout_formats', 'не указано')}
Рост: {user.get('height', 'не указано')} см, Вес: {user.get('weight', 'не указано')} кг, Возраст: {user.get('age', 'не указано')}, Пол: {user.get('gender', 'не указано')}

{split_info}
{superset_info}
{workout_history_info}
{exercises_info}

ТРЕБОВАНИЯ К JSON:
Верни объект такого вида:
{{
  "version": 1,
  "title": "Короткий заголовок тренировки",
  "muscle_groups": ["Спина", "Плечи"],
  "exercises": [
    {{"name": "Название упражнения", "weight_kg": 4, "sets": 3, "reps": 12}}
  ],
  "calories_burned": 320,
  "wellbeing_advice": "Короткий полезный совет с учётом самочувствия (1-2 предложения)"
}}

ОГРАНИЧЕНИЯ:
- muscle_groups: 1–3 группы мышц, которые безопасны и уместны при самочувствии пользователя (выбирай сам)
- exercises: 5–8 упражнений
- sets: 2–6
- reps: одно из значений: 5, 8, 10, 12, 15
- weight_kg: одно из значений: 2, 4, 6, 8, 10, 12, 16, 20, 25, 30, 40, 50 (если вес не нужен — ставь 0)
- calories_burned: целое число, оценка сожженных калорий за тренировку
- wellbeing_advice: короткий совет (1-2 предложения) для улучшения самочувствия с учётом описанной проблемы пользователя. Может включать рекомендации по питью воды, отдыху, разминке, питанию, сну или другие полезные советы для данной ситуации.

ПРАВИЛА:
- Не включай упражнения, которые могут ухудшить состояние, усилить боль или нагрузить травмированную область
- Предлагай безопасные альтернативы (вариации, меньшая амплитуда, изометрика, мобилити), если нужно
- Учитывай оборудование и ограничения
- По возможности избегай повторов из последних тренировок
- В массиве exercises должны быть ТОЛЬКО упражнения. НЕ добавляй советы, подсказки, комментарии или рекомендации в exercises. Для советов используй поле wellbeing_advice
- Возвращай ТОЛЬКО JSON (одна строка или много строк — не важно)"""

def build_manual_workout_infer_prompt(user: dict, exercises: list[dict[str, object]]) -> str:
    # На входе exercises: [{"name": "...", "weight_kg": 0|int, "sets": 0|int, "reps": 0|int}, ...]
    # ВАЖНО: мы НЕ меняем упражнения, а только определяем метаданные тренировки.
    return f"""Ты профессиональный фитнес-тренер. Проанализируй выполненную тренировку и верни СТРОГО JSON (без Markdown, без пояснений).

ДАННЫЕ ПОЛЬЗОВАТЕЛЯ:
Пол: {user.get('gender', 'не указано')}
Цель: {user.get('goal', 'не указано')}
Уровень: {user.get('level', 'не указано')}
Оборудование: {user.get('equipment', 'не указано')}

ВЫПОЛНЕННЫЕ УПРАЖНЕНИЯ (как ввёл пользователь):
{json.dumps(exercises, ensure_ascii=False)}

ТРЕБОВАНИЯ К JSON:
Верни объект такого вида:
{{
  "title": "Короткий заголовок тренировки (до 60 символов)",
  "muscle_groups": ["Спина", "Плечи"],
  "calories_burned": 320
}}

ПРАВИЛА:
- muscle_groups: 1–3 группы мышц по смыслу упражнений (без лишнего)
- calories_burned: целое число, приблизительная оценка
- Не изменяй и не дополняй список упражнений
- Верни ТОЛЬКО JSON"""


def build_workout_structured_prompt(
    user: dict,
    muscle_group: str,
    split_info: str,
    superset_info: str,
    workout_history_info: str,
    exercises_info: str,
) -> str:
    def _parse_selected_groups(value: str) -> list[str]:
        parts = [p.strip() for p in value.split(",")]
        return [p for p in parts if p]

    selected_groups = _parse_selected_groups(muscle_group)
    is_multi_group = len(selected_groups) > 1
    selected_groups_text = ", ".join(selected_groups) if selected_groups else muscle_group

    muscle_groups_constraints = (
        f"- muscle_groups: 2–4 группы мышц (из списка: {selected_groups_text}), без добавления других"
        if is_multi_group
        else f"- muscle_groups: 1–2 группы мышц, соответствующие дню тренировки: {selected_groups_text}"
    )

    balance_rule = (
        f"- Сбалансируй упражнения между выбранными группами мышц: {selected_groups_text}"
        if is_multi_group
        else ""
    )

    allowed_rule = (
        f"- Не добавляй упражнения на другие группы мышц, кроме: {selected_groups_text}"
        if is_multi_group
        else f"- Не добавляй упражнения на другие группы мышц, кроме {selected_groups_text}"
    )

    muscle_groups_example = json.dumps(selected_groups, ensure_ascii=False)

    return f"""Ты профессиональный фитнес-тренер. Составь тренировку СТРОГО в формате JSON (без Markdown, без пояснений).

ДАННЫЕ ПОЛЬЗОВАТЕЛЯ:
Цель: {user.get('goal', 'не указано')}
Уровень: {user.get('level', 'не указано')}
Ограничения: {user.get('health_issues', 'нет')}
Место занятий: {user.get('location', 'не указано')}
Частота тренировок (из анкеты): {user.get('workouts_per_week', 'не указано')} раз/нед
Время на тренировку: {user.get('workout_duration', 'не указано')}
Оборудование: {user.get('equipment', 'не указано')}
Форматы: {user.get('workout_formats', 'не указано')}
Рост: {user.get('height', 'не указано')} см, Вес: {user.get('weight', 'не указано')} кг, Возраст: {user.get('age', 'не указано')}, Пол: {user.get('gender', 'не указано')}

{split_info}
{superset_info}
{workout_history_info}
{exercises_info}

ТРЕБОВАНИЯ К JSON:
Верни объект такого вида:
{{
  "version": 1,
  "title": "Короткий заголовок тренировки",
  "muscle_groups": {muscle_groups_example},
  "exercises": [
    {{"name": "Название упражнения", "weight_kg": 4, "sets": 3, "reps": 12}}
  ],
  "calories_burned": 320
}}

ОГРАНИЧЕНИЯ:
{muscle_groups_constraints}
- exercises: 5–8 упражнений
- sets: 2–6
- reps: одно из значений: 5, 8, 10, 12, 15
- weight_kg: одно из значений: 2, 4, 6, 8, 10, 12, 16, 20, 25, 30, 40, 50 (если вес не нужен — ставь 0)
- calories_burned: целое число, оценка сожженных калорий за тренировку

ПРАВИЛА:
{allowed_rule}
{balance_rule}
- Все упражнения в тренировке должны быть УНИКАЛЬНЫМИ. Никогда не повторяй одно и то же упражнение дважды
- Учитывай оборудование и ограничения
- Избегай повторов из последних тренировок, если возможно
- В массиве exercises должны быть ТОЛЬКО упражнения. НЕ добавляй советы, подсказки, комментарии или рекомендации в exercises
- Возвращай ТОЛЬКО JSON (одна строка или много строк — не важно)"""


def build_workout_single_exercise_prompt(
    user: dict,
    muscle_group: str,
    superset_info: str,
    workout_history_info: str,
    existing_exercise_names: list[str],
) -> str:
    existing = ", ".join(existing_exercise_names[:12])
    return f"""Ты профессиональный фитнес-тренер. Сгенерируй ОДНО упражнение СТРОГО в формате JSON (без Markdown, без пояснений).

ДАННЫЕ ПОЛЬЗОВАТЕЛЯ:
Цель: {user.get('goal', 'не указано')}
Уровень: {user.get('level', 'не указано')}
Ограничения: {user.get('health_issues', 'нет')}
Место занятий: {user.get('location', 'не указано')}
Время на тренировку: {user.get('workout_duration', 'не указано')}
Оборудование: {user.get('equipment', 'не указано')}
Форматы: {user.get('workout_formats', 'не указано')}

Сегодня тренируем: {muscle_group}.
{superset_info}
{workout_history_info}

ЗАПРЕЩЁННЫЕ УПРАЖНЕНИЯ (НИКОГДА не возвращай ни одно из них, даже с изменённым названием): {existing}

ФОРМАТ JSON:
{{
  "name": "Название упражнения",
  "weight_kg": 4,
  "sets": 3,
  "reps": 12
}}

ОГРАНИЧЕНИЯ:
- ТОЛЬКО на группы мышц: {muscle_group}
- sets: 2–6
- reps: одно из значений: 5, 8, 10, 12, 15
- weight_kg: одно из значений: 2, 4, 6, 8, 10, 12, 16, 20, 25, 30, 40, 50 (если вес не нужен — ставь 0)

ПРАВИЛА:
- Учитывай оборудование и ограничения
- Возвращай ТОЛЬКО JSON"""

def build_workout_prompt(
    user: dict,
    muscle_group: str,
    exercise_count: int,
    tip_type: str,
    split_info: str,
    superset_info: str,
    workout_history_info: str,
    exercises_info: str
) -> str:
    return f"""Ты - профессиональный фитнесс-тренер. Сегодня у пользователя день тренировки: {muscle_group}. 
Составь тренировку только для этой группы мышц, не добавляй упражнения на другие группы. 
Учитывай цель, уровень, ограничения, частоту тренировок, время на тренировку, доступное оборудование, предпочитаемые форматы, физические параметры и место занятий.

Цель: {user.get('goal', 'не указано')}. 
Уровень: {user.get('level', 'не указано')}. 
Ограничения: {user.get('health_issues', 'нет')}. 
Место занятий: {user.get('location', 'не указано')}. 
Частота тренировок (из анкеты): {user.get('workouts_per_week', 'не указано')} раз в неделю. 
Время на тренировку: {user.get('workout_duration', 'не указано')}. 
Доступное оборудование: {user.get('equipment', 'не указано')}. 
Предпочитаемые форматы тренировок: {user.get('workout_formats', 'не указано')}. 
Рост: {user.get('height', 'не указано')} см. Вес: {user.get('weight', 'не указано')} кг. Возраст: {user.get('age', 'не указано')}. Пол: {user.get('gender', 'не указано')}. 

{split_info}
{superset_info}
{workout_history_info}
{exercises_info}

ВАЖНЫЕ ОГРАНИЧЕНИЯ:
• Используй только указанное оборудование: {user.get('equipment', 'собственный вес')}
• Придерживайся указанного формата тренировки: {user.get('workout_formats', 'классическая силовая')}
• Уложись в указанное время: {user.get('workout_duration', '30-45 минут')}
• По возможности избегай повторов упражнений из списка выше, но если базовое упражнение необходимо для прогрессии, можешь использовать его с вариацией

Сгенерируй ровно {exercise_count} упражнений (последнее — кардио или заминка). Сделай тренировку максимально разнообразной внутри выбранной группы мышц.
Для каждого упражнения обязательно указывай вес (даже если это собственный вес — пиши явно).

Форматируй ответ строго так:
Совет дня: [Персонализированный совет о {tip_type}, основанный на цель ({user.get('goal', 'не указано')}), уровень подготовки ({user.get('level', 'не указано')}) и особенности пользователя. Совет должен быть практичным и применимым]

План тренировки на сегодня: {muscle_group}

Для каждого упражнения используй такой формат:
[Номер]. [Название упражнения]
[кол-во подходов × повторений — вес]
*Комментарий по технике или совет*

Последнее упражнение всегда кардио или заминка, указывай его в том же формате.

Не добавляй лишних пояснений вне структуры плана."""


KBJU_CALCULATION_SYSTEM_PROMPT = "Ты профессиональный диетолог-нутрициолог. Рассчитываешь персонализированные нормы КБЖУ. Возвращай только JSON."

def build_kbju_calculation_prompt(user: dict, nutrition_brief: dict) -> str:
    """Build prompt for AI-based KBJU calculation."""
    return f"""
Рассчитай персонализированные дневные нормы КБЖУ для пользователя.

ДАННЫЕ ПОЛЬЗОВАТЕЛЯ:
- Пол: {user.get('gender', 'не указан')}
- Возраст: {user.get('age', 'не указан')} лет
- Рост: {user.get('height', 'не указан')} см
- Вес: {user.get('weight', 'не указан')} кг
- Уровень подготовки: {user.get('level', 'не указан')}
- Частота тренировок: {user.get('workouts_per_week', 'не указана')} раз в неделю
- Цель фитнеса: {user.get('goal', 'не указана')}

БРИФ ПЛАНА ПИТАНИЯ:
- Цель питания: {nutrition_brief.get('nutrition_goal', 'не указана')}
- Ограничения в питании: {nutrition_brief.get('dietary_restrictions', 'нет')}
- Предпочтения в еде: {nutrition_brief.get('meal_preferences', 'нет')}
- Время на готовку: {nutrition_brief.get('cooking_time', 'не указано')}
- Бюджет: {nutrition_brief.get('budget', 'не указан')}

МЕТОДИКА РАСЧЁТА:
1. Рассчитай BMR по формуле Миффлина-Сан Жеора
2. Определи TDEE с учётом уровня активности
3. Скорректируй калории под цель:
   - Похудение: дефицит 400-500 ккал (но не менее 1200 ккал для женщин, 1500 для мужчин)
   - Набор массы: профицит 200-400 ккал
   - Поддержание: TDEE без изменений
4. Рассчитай БЖУ:
   - Белки: 1.6-2.2 г/кг веса (выше при похудении для сохранения мышц)
   - Жиры: минимум 0.8-1.0 г/кг для здоровья гормональной системы
   - Углеводы: остаток калорий

ВАЖНО:
- Учитывай ВСЕ данные пользователя и брифа
- Если данные неполные — используй средние значения (вес 70кг, рост 170см, возраст 30)
- Округляй до целых чисел
- Белки и углеводы = 4 ккал/г, жиры = 9 ккал/г

Верни JSON строго в формате:
{{
  "target_calories": 2000,
  "target_proteins": 140,
  "target_fats": 65,
  "target_carbs": 220,
  "reasoning": "Краткое обоснование расчёта (1-2 предложения)"
}}

Возвращай ТОЛЬКО JSON, без markdown и пояснений.
""".strip()


FOOD_ANALYSIS_PROMPT = """Определи, что изображено на фото, и оцени калорийность блюда. 
Верни JSON вида: {"description": "...", "calories": ..., "proteins": ..., "fats": ..., "carbs": ...}. 
Обязательно укажи БЖУ (белки, жиры, углеводы) в граммах. 
Отвечай всегда только на русском языке."""


FOOD_CLARIFICATION_PROMPT = """Пользователь прислал фото еды. Вот его уточнение: '{clarification}'. 
Определи, что изображено на фото, и оцени калорийность блюда с учётом уточнения. 
Верни JSON вида: {{"description": "...", "calories": ..., "proteins": ..., "fats": ..., "carbs": ...}}. 
Обязательно укажи БЖУ (белки, жиры, углеводы) в граммах. 
Отвечай всегда только на русском языке."""


DAILY_MENU_SYSTEM_PROMPT = "Ты профессиональный диетолог. Создаёшь разнообразные рационы на день. Возвращай только JSON."

DAILY_MENU_SYSTEM_PROMPT_LEGACY = "Ты профессиональный диетолог. Создаёшь разнообразные рационы на день. Отвечай всегда только на русском языке."

SHOPPING_LIST_SYSTEM_PROMPT = "Ты помощник по составлению списков продуктов. Отвечай всегда только на русском языке."


def build_daily_menu_structured_prompt(user: dict, nutrition_plan: dict, day_name: str = "") -> str:
	"""Build prompt for structured JSON menu generation for a single day."""
	user_info = f"""
Физические данные:
- Пол: {user.get('gender', 'не указан')}
- Возраст: {user.get('age', 'не указан')}
- Рост: {user.get('height', 'не указан')} см
- Вес: {user.get('weight', 'не указан')} кг
- Цель фитнеса: {user.get('goal', 'не указана')}
- Уровень активности: {user.get('level', 'не указан')}
- Частота тренировок: {user.get('workouts_per_week', 'не указана')} раз в неделю

Параметры плана питания:
- Цель питания: {nutrition_plan.get('nutrition_goal', '')}
- Ограничения: {nutrition_plan.get('dietary_restrictions', '')}
- Предпочтения: {nutrition_plan.get('meal_preferences', '')}
- Время на готовку: {nutrition_plan.get('cooking_time', '')}
- Бюджет: {nutrition_plan.get('budget', '')}
""".strip()

	target_calories = nutrition_plan.get('target_calories', 2000)
	target_proteins = nutrition_plan.get('target_proteins', 100)
	target_fats = nutrition_plan.get('target_fats', 70)
	target_carbs = nutrition_plan.get('target_carbs', 250)
	
	day_context = f"Это рацион на {day_name}. " if day_name else ""

	return f"""
Создай разнообразный рацион на день для пользователя. {day_context}

{user_info}

Целевые показатели на день:
- Калории: {target_calories} ккал
- Белки: {target_proteins} г
- Жиры: {target_fats} г
- Углеводы: {target_carbs} г

Верни JSON строго в следующем формате:
{{
  "target_calories": {target_calories},
  "target_proteins": {target_proteins},
  "target_fats": {target_fats},
  "target_carbs": {target_carbs},
  "sections": [
    {{
      "type": "breakfast",
      "title": "Завтрак",
      "time_range": "7:00-9:00",
      "items": [
        {{"name": "Название блюда", "calories": 350, "proteins": 15, "fats": 10, "carbs": 45}}
      ]
    }},
    {{
      "type": "lunch",
      "title": "Обед",
      "time_range": "12:00-14:00",
      "items": [
        {{"name": "Название блюда", "calories": 500, "proteins": 30, "fats": 15, "carbs": 60}}
      ]
    }},
    {{
      "type": "dinner",
      "title": "Ужин",
      "time_range": "18:00-20:00",
      "items": [
        {{"name": "Название блюда", "calories": 400, "proteins": 25, "fats": 12, "carbs": 40}}
      ]
    }},
    {{
      "type": "snacks",
      "title": "Перекусы",
      "time_range": "В течение дня",
      "items": [
        {{"name": "Название перекуса", "calories": 150, "proteins": 5, "fats": 3, "carbs": 20}}
      ]
    }}
  ],
  "tip_of_day": "Практичный совет по питанию"
}}

КРИТИЧЕСКИ ВАЖНО - ТОЧНОЕ СООТВЕТСТВИЕ КБЖУ:
1. Сумма калорий всех блюд за день ДОЛЖНА ТОЧНО равняться {target_calories} ккал (±50 ккал)
2. Сумма белков ДОЛЖНА ТОЧНО равняться {target_proteins} г (±5 г)
3. Сумма жиров ДОЛЖНА ТОЧНО равняться {target_fats} г (±5 г)
4. Сумма углеводов ДОЛЖНА ТОЧНО равняться {target_carbs} г (±10 г)
5. Рассчитай КБЖУ каждого блюда так, чтобы при суммировании получились целевые значения

ДОПОЛНИТЕЛЬНЫЕ ТРЕБОВАНИЯ:
6. Создай УНИКАЛЬНЫЕ блюда для этого дня
7. Учитывай все ограничения и предпочтения пользователя
8. Подбери блюда под бюджет и время готовки
9. Блюда должны быть реалистичными и выполнимыми
10. В каждой секции должно быть 1-3 блюда
11. Названия блюд на русском языке
12. Возвращай ТОЛЬКО JSON, без markdown и пояснений

ПРОВЕРКА: Перед ответом убедись, что сумма calories/proteins/fats/carbs всех блюд соответствует целевым значениям!
""".strip()


def build_weekly_menu_structured_prompt(user: dict, nutrition_plan: dict) -> str:
	"""Build prompt for structured JSON menu generation for 7 days."""
	user_info = f"""
Физические данные:
- Пол: {user.get('gender', 'не указан')}
- Возраст: {user.get('age', 'не указан')}
- Рост: {user.get('height', 'не указан')} см
- Вес: {user.get('weight', 'не указан')} кг
- Цель фитнеса: {user.get('goal', 'не указана')}
- Уровень активности: {user.get('level', 'не указан')}
- Частота тренировок: {user.get('workouts_per_week', 'не указана')} раз в неделю

Параметры плана питания:
- Цель питания: {nutrition_plan.get('nutrition_goal', '')}
- Ограничения: {nutrition_plan.get('dietary_restrictions', '')}
- Предпочтения: {nutrition_plan.get('meal_preferences', '')}
- Время на готовку: {nutrition_plan.get('cooking_time', '')}
- Бюджет: {nutrition_plan.get('budget', '')}
""".strip()

	target_calories = nutrition_plan.get('target_calories', 2000)
	target_proteins = nutrition_plan.get('target_proteins', 100)
	target_fats = nutrition_plan.get('target_fats', 70)
	target_carbs = nutrition_plan.get('target_carbs', 250)

	return f"""
Создай разнообразное меню на НЕДЕЛЮ (7 дней) для пользователя.

{user_info}

Целевые показатели НА КАЖДЫЙ ДЕНЬ:
- Калории: {target_calories} ккал
- Белки: {target_proteins} г
- Жиры: {target_fats} г
- Углеводы: {target_carbs} г

Верни JSON массив из 7 объектов (понедельник-воскресенье):
[
  {{
    "day_of_week": 0,
    "day_name": "Понедельник",
    "sections": [
      {{
        "type": "breakfast",
        "title": "Завтрак",
        "time_range": "7:00-9:00",
        "items": [{{"name": "Блюдо", "calories": 350, "proteins": 15, "fats": 10, "carbs": 45}}]
      }},
      {{
        "type": "lunch",
        "title": "Обед",
        "time_range": "12:00-14:00",
        "items": [{{"name": "Блюдо", "calories": 500, "proteins": 30, "fats": 15, "carbs": 60}}]
      }},
      {{
        "type": "dinner",
        "title": "Ужин",
        "time_range": "18:00-20:00",
        "items": [{{"name": "Блюдо", "calories": 400, "proteins": 25, "fats": 12, "carbs": 40}}]
      }},
      {{
        "type": "snacks",
        "title": "Перекусы",
        "time_range": "В течение дня",
        "items": [{{"name": "Перекус", "calories": 150, "proteins": 5, "fats": 3, "carbs": 20}}]
      }}
    ],
    "tip_of_day": "Совет дня"
  }},
  ... // еще 6 дней (вторник=1, среда=2, четверг=3, пятница=4, суббота=5, воскресенье=6)
]

КРИТИЧЕСКИ ВАЖНО - ТОЧНОЕ СООТВЕТСТВИЕ КБЖУ НА КАЖДЫЙ ДЕНЬ:
1. Сумма калорий всех блюд за КАЖДЫЙ день ДОЛЖНА ТОЧНО равняться {target_calories} ккал (±50 ккал)
2. Сумма белков за КАЖДЫЙ день ДОЛЖНА ТОЧНО равняться {target_proteins} г (±5 г)
3. Сумма жиров за КАЖДЫЙ день ДОЛЖНА ТОЧНО равняться {target_fats} г (±5 г)
4. Сумма углеводов за КАЖДЫЙ день ДОЛЖНА ТОЧНО равняться {target_carbs} г (±10 г)
5. Рассчитай КБЖУ каждого блюда так, чтобы при суммировании за день получились целевые значения
6. НЕ генерируй меню с суммой калорий меньше или больше целевого — это строгое требование

ДОПОЛНИТЕЛЬНЫЕ ТРЕБОВАНИЯ:
7. Создай УНИКАЛЬНЫЕ блюда на каждый день - НЕ повторяй одинаковые блюда в разные дни
8. Учитывай все ограничения и предпочтения пользователя
9. Подбери блюда под бюджет и время готовки
10. В каждой секции должно быть 1-3 блюда
11. Названия блюд на русском языке
12. day_of_week: 0=понедельник, 1=вторник, ..., 6=воскресенье
13. Возвращай ТОЛЬКО JSON массив, без markdown и пояснений

ОБЯЗАТЕЛЬНАЯ ПРОВЕРКА ПЕРЕД ОТВЕТОМ:
- Для каждого дня сложи calories всех items во всех sections
- Убедись что сумма = {target_calories} (±50)
- То же самое для proteins, fats, carbs
- Если суммы не сходятся — скорректируй КБЖУ блюд
""".strip()


def build_daily_menu_prompt(user: dict, nutrition_plan: dict) -> str:
	"""Legacy prompt for text-based menu generation."""
	user_info = f"""
Физические данные:
- Пол: {user.get('gender', 'не указан')}
- Возраст: {user.get('age', 'не указан')}
- Рост: {user.get('height', 'не указан')} см
- Вес: {user.get('weight', 'не указан')} кг
- Цель фитнеса: {user.get('goal', 'не указана')}
- Уровень активности: {user.get('level', 'не указан')}
- Частота тренировок: {user.get('workouts_per_week', 'не указана')} раз в неделю

Параметры плана питания:
- Цель питания: {nutrition_plan.get('nutrition_goal', '')}
- Ограничения: {nutrition_plan.get('dietary_restrictions', '')}
- Предпочтения: {nutrition_plan.get('meal_preferences', '')}
- Время на готовку: {nutrition_plan.get('cooking_time', '')}
- Бюджет: {nutrition_plan.get('budget', '')}
""".strip()

	target_kbju = ""
	if nutrition_plan.get("target_calories"):
		target_kbju = f"""
Целевые показатели:
- Калории: {nutrition_plan.get('target_calories')} ккал
- Белки: {nutrition_plan.get('target_proteins', 'не указано')} г
- Жиры: {nutrition_plan.get('target_fats', 'не указано')} г
- Углеводы: {nutrition_plan.get('target_carbs', 'не указано')} г
""".strip()

	return f"""
Создай НОВЫЙ разнообразный рацион на день для пользователя.

{user_info}
{target_kbju}

Создай рацион в следующем формате:

📊 **ЦЕЛЕВЫЕ ПОКАЗАТЕЛИ НА ДЕНЬ:**
Калории: {nutrition_plan.get('target_calories', 'рассчитай')} ккал
Белки: {nutrition_plan.get('target_proteins', 'рассчитай')} г
Жиры: {nutrition_plan.get('target_fats', 'рассчитай')} г
Углеводы: {nutrition_plan.get('target_carbs', 'рассчитай')} г

🥗 **РАЦИОН НА СЕГОДНЯ:**

**ЗАВТРАК (7:00-9:00):**
• [Блюдо 1] - [калории] ккал, Б[белки]г/Ж[жиры]г/У[углеводы]г
• [Блюдо 2] - [калории] ккал, Б[белки]г/Ж[жиры]г/У[углеводы]г

**ОБЕД (12:00-14:00):**
• [Блюдо 1] - [калории] ккал, Б[белки]г/Ж[жиры]г/У[углеводы]г
• [Блюдо 2] - [калории] ккал, Б[белки]г/Ж[жиры]г/У[углеводы]г

**УЖИН (18:00-20:00):**
• [Блюдо 1] - [калории] ккал, Б[белки]г/Ж[жиры]г/У[углеводы]г
• [Блюдо 2] - [калории] ккал, Б[белки]г/Ж[жиры]г/У[углеводы]г

**ПЕРЕКУСЫ:**
• [Перекус 1] - [калории] ккал
• [Перекус 2] - [калории] ккал

💡 **СОВЕТ ДНЯ:**
[Практичный совет по питанию или готовке на сегодня]

ВАЖНО:
1. Каждый раз создавай РАЗНЫЕ блюда для разнообразия
2. Учитывай все ограничения и предпочтения
3. Подбери блюда под бюджет и время готовки
4. Обязательно укажи КБЖУ для каждого блюда
5. Блюда должны быть реалистичными и выполнимыми
""".strip()


def build_shopping_list_prompt(daily_menu: str) -> str:
	return f"""
На основе следующего рациона создай удобный список продуктов для покупки:

{daily_menu}

Создай список в формате:

🛒 **СПИСОК ПРОДУКТОВ:**

**МЯСО И РЫБА:**
• [Продукт] - [количество]
• [Продукт] - [количество]

**МОЛОЧНЫЕ ПРОДУКТЫ:**
• [Продукт] - [количество]
• [Продукт] - [количество]

**ОВОЩИ И ФРУКТЫ:**
• [Продукт] - [количество]
• [Продукт] - [количество]

**КРУПЫ И МАКАРОНЫ:**
• [Продукт] - [количество]

**ДРУГОЕ:**
• [Продукт] - [количество]

💰 **ПРИМЕРНАЯ СТОИМОСТЬ:** [сумма] ₽

ТРЕБОВАНИЯ:
1. Указывай конкретные количества (граммы, штуки, пачки)
2. Группируй продукты по отделам магазина
3. Учитывай что некоторые продукты могут быть дома
4. Добавь примерную стоимость покупки
5. Только продукты из рациона выше
""".strip()

